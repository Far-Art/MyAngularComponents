import { DatePlaceholderPipe } from './date-placeholder.pipe';

describe('DatePlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
